import {DirectionEnum} from "../enums/direction.enum";

export const DEFAULT_PAGINATOR_PAGE: number = 0;
export const DEFAULT_PAGINATOR_SIZE: number = 10;

export const DEFAULT_PAGINATOR_DIRECTION: DirectionEnum = DirectionEnum.ASC;

export const DEFAULT_PAGE_SIZE_LIST: (string | number)[] = [10, 20, 40, 80, 100];

export const TOKEN_PREFIX: string = 'Bearer ';
